`jami-imexcentral` implements the publicationFetcher from jami-bridges/bridges-core to fetch publications from imex.

Seems to read data from IMEx but there is an update() method so can write to it too?

